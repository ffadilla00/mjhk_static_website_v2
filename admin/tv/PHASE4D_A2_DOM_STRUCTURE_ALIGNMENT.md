# MJHK TV Phase 4D-A2 — DOM Structure Alignment

This hotfix removes the custom `.admin-shell` wrapper and fixes the
`.dedicated-page` closing tag so Dedicated Screens uses the same body structure
as the locked TV CMS pages.

No JavaScript, Supabase, CRUD, schema, or revision behavior is changed.
