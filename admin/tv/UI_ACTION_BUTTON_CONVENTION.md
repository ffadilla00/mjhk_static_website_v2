# MJHK TV CMS UI Convention — Action Buttons

Locked convention:

- `btn-save` → green → save/persist/confirm write action
- `btn-cancel` → orange → cancel/abort/back-out action

Do not use neutral gray for explicit save actions.
Do not use red for cancel unless the action is destructive; destructive actions keep their dedicated danger styling.
